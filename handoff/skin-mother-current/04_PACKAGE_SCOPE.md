# Skin Mother — Full Package Scope — 2026-09-12

The full handoff package generated from `feature/b24-80days-simple-placement-r9` is intended to let the next session continue immediately without guessing which line is authoritative.

Included:

- the complete current R9 working tree,
- the frozen Generic Mother01 files already inherited into this line,
- R1–R6 80 DAYS runtime/workbench files present on the branch,
- current diagnostic/support files present on the branch,
- workflows and QA definitions present on the branch,
- source-lock and handoff documents in `handoff/skin-mother-current/`,
- the public-preview delivery rule and repository instructions.

Not treated as active product code:

- R7 automatic candidate/window registration,
- R8 projective/homography registration.

Those dead ends are preserved by exact branch/commit references in the handoff documents rather than being promoted into the new working path.

The package excludes previously generated ZIP packages to avoid recursive/nested package growth. It includes the underlying files instead.

The package does not claim that 80 DAYS is visually accepted. Its purpose is exact continuation from the current state.
