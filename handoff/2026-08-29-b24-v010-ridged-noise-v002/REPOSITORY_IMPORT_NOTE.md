# Repository import note

This directory records the attached `AIRCRAFT_B24_FULL_HANDOFF_2026-08-29_V010_RIDGED_NOISE_V002.zip` for Draft PR #14 on `research/b24-engineering-drawings-cad-v1`.

The original package contains one 130,636,401-byte baseline ZIP. That file is intentionally not copied into the repository because it exceeds GitHub's regular Git single-file limit. Its original path and SHA-256 remain recorded in `records/MANIFEST.json`, `records/SHA256SUMS.txt`, and `baseline/BASELINE_POINTER.json`. All other package payload files are included here.

This repository import does not change the package's approval state: `visualApproved` and `ridgedLayerApproved` remain `false`.
