# Start here

This branch is backup-only. Do not develop directly inside the numbered snapshot directories.

Read `BACKUP_MANIFEST.json` for exact commit and tree identities. Read `RESTART_PLAN.md` for the clean continuation branch and current work order.

Recovery rule: restore one exact snapshot directory or one exact source commit. Never combine old runtime code, deprecated experiments, rejected visual work, or unrelated branches by default.

Current open production issues are intentionally preserved as open: propeller geometry is absent from the static skin subset, the bomb-bay source closed endpoint retains an approximately 250.75 mm centre opening, and the next livery workbench still needs fixed orthographic views plus unrestricted orbit and zoom.
