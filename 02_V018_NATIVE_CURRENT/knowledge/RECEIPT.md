# B24 V018 执行与交付回执

日期：2026-09-05。执行会话：B24-NATIVE-DISTILLATION-R1。用户批准按清除原贴图、重建动画控制、随后推进几何配方的路线执行。本轮未使用 Blender 或 Blender MCP。

## 本轮已实际完成

新生产分支为 `feature/b24-native-distillation-r1`。读取来源固定为 `b8b2a6c441fa9a9b7e0831f4217511b4968d266e`；源码实现提交为 `dd0c12eda9143cb9fe456e86e565ee992fd2dab7`；经过自动构建和浏览器验证的完整运行提交为 `f3a8e9edc3c2dc542d680aee469467e95211a31f`。

飞机主体压缩数据由 8,917,196 字节变为 4,061,477 字节，减少 4,855,719 字节，即 54.453429%。分段数由18变为9。新解压数值载荷为8,131,628字节，SHA-256为 `f5ff859a7ff0e38112fa099d8c7d3a4cd8e859434701fb4dd9d81629374c5e3e`。这些数值仅比较飞机数据，不包含渲染库的传输，也不等同于用户电脑加载时间同比下降。

原18张嵌入图片、原UV块、原贴图/材质引用和未运行的完整动画通道表已退出新载荷。348个网格的顶点、法线和三角形索引值完成1044组逐项相等检查；索引无损转16位，顶点及法线没有量化，没有减面。现阶段仍保留数值网格，整机几何参数配方尚未完成。

新的 `MotionSystem` 使用1784个初始化姿态、起落架与弹舱标量控制、1037个绑定、779条共享控制曲线和四个独立桨轴控制器。曲线仍由原有动画提取，当前成果属于运动运行系统迁移，不能推导真实机构几何与运动规律均已重建。先前发现恒定非单位四元数折叠会改变原slerp结果，已保留这些旋转曲线，未放宽检查容差。

514组姿态与控制状态共进行了916976次节点比较。最大局部分量误差为1.1102230246251565e-16，最大世界矩阵元素误差为8.881784197001252e-16；旋转步进、暂停、复位及非法输入检查通过。

## 网页与连续流程验证

源码与连续验证运行：AIRCRAFT Actions `33971144973`。本地服务器上的真实 Chromium 浏览器检查76/76通过。随后桌面1440x900和手机尺寸390x844各连续运行两次330秒任务，速率1倍、无时间轴跳转。每个视口均观察到18个阶段、8次投放和8次落点事件，无运行异常，循环后场景回到起始状态。桌面记录672.595秒墙钟时间，手机尺寸记录663.894秒；原始记录保存在 `reports/SOAK_1440.json` 与 `reports/SOAK_390.json`。

浏览器使用GitHub Linux上的Chromium与SwiftShader。该环境渲染很慢，桌面记录431帧、手机尺寸记录674帧。这些结果证明本次连续状态检查通过，不代表实机高帧率或用户手机的性能。用户设备的首次加载耗时尚未测量。

新入口： https://haihao0307.github.io/guilin-dem-pipeline/aircraft/b24-native-data/

发布仓库 `guilin-dem-pipeline` 的 `gh-pages` 提交为 `6c34aa5d872e22f4b4eaa69bb2ca69c44789129f`。发布目录 `aircraft/b24-native-data` 的树身份为 `f3c9569088d052f2f47c3a30871f814b7705f481`，与已测运行目录一致。远端差异仅该新目录27个新增文件。V017、V017.1及其他生产工作台均未修改。

公网验证运行：guilin-dem-pipeline Actions `33971449639`。27个实际在线文件逐个核对匹配，公网真实浏览器检查76/76通过；已检查公网截图中飞机与界面正常呈现。源报告来自artifact `9971161312`。`reports/PUBLIC_BYTES.json` 与 `reports/PUBLIC_BROWSER_QA.json` 保存相同JSON数据，空白格式经过压缩。原报告SHA-256分别为 `f94daaa8eef469f1f339018da9f4f435e337ce07992482dd31b1eb05c9cf9dd0`、`bc51f9347ff93c82dc2d278067c4e3d0f17ce63e24211d6535507ad24e124559`。

## 下载与文件身份

已测源码包artifact `9971003982`，ZIP SHA-256 `52f1b33dca43b9285cb5ff48415f86dbd48e6bd769e286823b701a9c0a016073`，本地下载后验证ZIP完整性、源码及运行数据。该包是发布前的已测运行快照，不含后续新增的交付回执和连续测试归档。源码包中的测试截图属于证据，未进入runtime。

完整连续报告由artifact `9971178991` 与 `9971176016`恢复入Git，保留原字节。其JSON SHA-256分别为 `7db40a6750abbd72dca054903a4de091b9d39e36e2151ca72f402c162f3dfb44` 与 `0c9239fbc2a2e8141663dcbf3e59273c6b0c80ff307bbbdb52cc2d28517b7252`。证据汇集分支只用于归档，没有整支合入生产分支。

## 仍未完成的工作

整机曲面配方、桨叶完整参数模型、前轮及连杆的工程校正、弹舱库存与清空绑定，以及有历史依据的蒙皮标记均仍在待完成清单中。已对一片桨叶进行多种截面表达试验，闭合曲线试验未通过局部形状误差门限，未绑定到生产模型。可重放的源码和失败记录保存在 `experiments/propeller-profile-r1/`。这项试验不等于几何生产完成。

你认可的V017保留为恢复基线，V018仍为待人工视觉确认的候选，visualAcceptance=false、productionReady=false。天气、云和雾继续不接入。原始图片资产已移除；环境反射、阴影和任务烟雾仍生成临时渲染缓冲，未声称GPU中没有纹理对象。

本轮实际启动并完成的是源码构建、浏览器核验和连续播放自动测试。没有启动整夜自主编写代码或自动完成余下曲面研究的代理，不能将已完成的测试记录解释为余下工程会在后台自行完成。
