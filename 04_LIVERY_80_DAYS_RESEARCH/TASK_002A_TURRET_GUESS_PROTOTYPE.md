# TASK 002A · 腹部球形炮塔猜测原型

## 目的

在精确模型审计完成前，先跑通一套明确标记为“猜测”的炮塔组件选择与临时独立化体系。这个原型只验证交互、层级组织和未来运动接口，不代表真实拆分结论。

## 当前上游猜测

1. 炮塔球壳大概率是独立节点、独立 primitive 或独立连通岛。
2. 左右两挺机枪大概率至少能够按几何连通岛或材质组分开。
3. 整套炮塔应围绕腹部连接点水平旋转。
4. 双枪应围绕共同俯仰轴同步运动。
5. 炮塔收放能力暂不假定，仅保留可选接口。
6. 整套炮塔未来可以像炸弹一样解除父子关系，但当前只做无物理的临时脱离预览。

所有猜测都必须在 UI 和 JSON 中标记为 `hypothesis`，并带 `confidence` 与 `evidence`。

## 组件选择

建立可选择的组件槽位：

- `assembly` 整套腹部炮塔
- `ballShell` 球壳
- `gunPair` 双枪总成
- `gunLeft` 左枪
- `gunRight` 右枪
- `barrels` 炮管组
- `interior` 内部机构与瞄准装置
- `mountRing` 炮塔座圈或连接结构
- `adjacentFuselage` 相邻机身蒙皮

每个槽位可以包含一个或多个源节点或网格。

## 三层候选识别

### 第一层 · 名称与材质

使用节点名、网格名、材质名匹配：

`turret`, `ball`, `ventral`, `belly`, `gun`, `barrel`, `muzzle`, `mount`, `ring`, `lower`

名称匹配只能产生候选，不得直接视为真实结论。

### 第二层 · 空间探针

围绕 v0.9.6 已知左右炮口坐标建立可视化探针：

- 左炮口 `[-0.26, -2.15, -0.55]`
- 右炮口 `[0.26, -2.15, -0.55]`
- 初始方向 `[0, -0.12, 0.99]`

按包围盒中心距离、与腹部中心的相对位置、几何尺寸和左右对称关系评分。

### 第三层 · 人工点选

允许用户在模型上点击网格，查看节点、父链、材质、包围盒与动画引用，然后手动归入某个组件槽位。

人工选择优先级高于自动猜测。保存到浏览器本地，并可导出 JSON。

## 操作模式

对当前选中的组件提供：

1. 高亮
2. 显示或隐藏
3. 透明显示
4. 爆炸视图
5. 水平旋转预览
6. 双枪俯仰预览
7. 临时独立化
8. 一键恢复
9. 导出组件映射

## 爆炸视图

使用滑块 `0..1`。所有位移只应用于克隆或临时容器。原始节点、矩阵和父子关系保持不变。

建议偏移方向：

- ballShell 向下
- gunLeft 向左下
- gunRight 向右下
- barrels 沿炮管方向前移
- interior 向后或向下
- mountRing 轻微向上
- adjacentFuselage 保持原位

## 临时独立化

创建：

```text
TurretHypothesisPreview
  TurretAttachRoot
    TurretYaw
      TurretBallShell
      GunElevation
        GunLeft
        GunRight
      SightAndInterior
```

实现方式：

- 复制候选对象，或使用 `Object3D.attach()` 保持世界变换进行临时 reparent。
- 默认保留原始对象并将其半透明或隐藏。
- 退出预览时销毁克隆并恢复原对象。
- 不写回 GLB。
- 不改变原动画。
- 不加入重力、碰撞、掉落或爆炸。

## 运动预览

### 水平旋转

- 控制对象：`TurretYaw`
- 范围：测试阶段 `-180°..180°`
- 默认：`0°`

### 双枪俯仰

- 控制对象：`GunElevation`
- 范围：测试阶段 `-70°..30°`
- 默认：`0°`
- 左右枪同步

### 左右枪独立测试

只提供一个“解除同步”调试开关，用于验证左右枪是否真正独立。正式系统默认保持同步。

## 选择器 UI

新增“炮塔猜测原型”面板：

- 识别方式：自动猜测 / 空间探针 / 人工选择
- 组件列表
- 每项包含节点数量、网格数量、confidence、evidence
- 操作按钮：高亮、隐藏、爆炸、临时独立、恢复
- 滑块：爆炸程度、水平旋转、俯仰
- 开关：左右枪解除同步
- 导出 `turret-hypothesis-map.json`
- 导入修正后的映射

## 输出 JSON

```json
{
  "schema": "haihao.aircraft/turret-hypothesis@1.0",
  "sourceModel": {
    "verified": false,
    "sha256": ""
  },
  "status": "hypothesis",
  "components": {
    "ballShell": {
      "nodes": [],
      "meshes": [],
      "confidence": 0,
      "evidence": []
    }
  },
  "manualOverrides": [],
  "safeForProduction": false
}
```

当精确模型验证通过后填入真实哈希。正式 TASK 002 审计结果可以覆盖这份猜测映射。

## 验收

1. 网站能进入“炮塔猜测原型”模式。
2. 自动候选、空间探针和人工点选都可工作。
3. 可以选择整套炮塔、球壳、双枪、左右枪、内部机构和相邻蒙皮。
4. 可以高亮、隐藏、爆炸分离、水平旋转、俯仰、临时独立化和恢复。
5. 所有操作非破坏性。
6. 所有自动结论显示猜测状态和 confidence。
7. 可以导出和导入组件映射。
8. 当前阶段没有坠落物理。
9. 原飞机模型、动画、涂装、起落架、螺旋桨和飞行逻辑不受影响。

完成后提交截图、操作说明和 JSON 示例，等待上游验收。
