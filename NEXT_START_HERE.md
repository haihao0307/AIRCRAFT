# Restart the B24 livery workbench

The full backup is complete at `f3b1f1a27b036df7d41ddda653ec6662ffe05653`. Continue only on `restart/b24-livery-workbench-clean-r1-20260907`.

Open `workbench/B24_LIVERY_WORKBENCH_R1_BASELINE.html` as the frozen visual baseline. It is the same verified static single-file payload previously delivered, now isolated in the clean restart line.

## Immediate work

First restore the four propeller assemblies and hubs from the fixed V018 compact geometry source. Add them as non-paint fit references without adding animation or changing aircraft scale. Then add fixed port, starboard, nose, tail, top, bottom, and three-quarter cameras while keeping unrestricted orbit, pan, zoom, and close inspection.

After those structural review functions pass, begin 80 DAYS livery placement from source-locked evidence. Treat left and right sides independently. Keep doors and movable surface identities separate so markings follow their parts later. Do not bring back original raster textures or discarded UV payloads.

The bomb-bay closure diagnosis is preserved, but its geometry correction is outside the first livery workbench increment. Do not hide the gap with a new cover object.

Each candidate must be a self-contained HTML stored in a fixed commit and delivered through raw.githack. Preserve the baseline file unchanged and create a new file for each review candidate.
