# Continue B24

The user explicitly accepted the V017 base on 2026-09-05 and authorized scoped cleanup, new skill adoption and missing presentation effects. See ACCEPTED_BASELINE.json for the exact source and public dependency identities.

Continue on feature/b24-v017-clean-restart-workbench. Read CURRENT.json and knowledge/SKILL.md, then verify the current HEAD before modifying files. runtime/ is the only active production entry. Its index and binary/vendor dependencies are materialized by tools/build.py and retained by successful verification.

V017.1 is an effects candidate. Keep the accepted V017 page untouched. Keep original geometry, source node parents, UV, mechanical tracks, four rotation axes, mission and airfield immutable. Do not assume the earlier learning-only guard applies to this newly authorized production change.

Weather Mother and fog will be connected only after their rebuild and a new integration decision. Do not revive old weather code. Recovery snapshots are for explicit recovery, never runtime fallback.

Tests and pending items are in knowledge/SKILL.md and reports when available. Failed or unrun tests must not be recorded as passed. User acceptance belongs to a specific version.
