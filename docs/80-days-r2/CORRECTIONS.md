# 80 DAYS · 原图修正 R2

2026-09-09。用户明确纠正 R1 的近似重绘与位置，并要求使用给定输入完成本次修正。本轮仅处理此实例，不代签或接管其他 Mother 的清理。

## 图案来源

- **名称与引号、骰子、嘴、眼部：**直接裁取用户透明 PNG 的原始像素；裁切只排除相邻的其他图案。所保留像素的 RGB 与 alpha 均未改变，没有替换字体、重画骰子或牙齿。骰子的分界细线、侧面及底面点保留。
- **嘴部颜色：**使用 PNG 原有红色口腔、黑色外缘和白色牙齿。R1 的黑嘴与一小块红色已退出本轮显示。
- **黄色炸弹：**从用户 B-17 JPEG 中直接分离黄色标记的像素轮廓和颜色，没有自行设计轮廓。单个原始区域仅 12×28 像素，放大仍受输入清晰度限制。排列沿用用户 PNG 中的 30 枚及其中间分组间隔，不把这个数登记为已核实的历史任务总数。
- **STAM 与 HUFF：**按对应照片的位置分别登记，字迹从照片分离，没有用另一种字体重写。HUFF 轮廓采用较清楚的 E04 局部，位置采用右侧飞行照片；不由此断言两个照片是同一天。
- **旗标：**使用给定 PNG 的图案；STAM 照片中可见行的 8 个位置分别登记。

原 PNG 原件文件为 `assets/80-days-r2/user-original.png`，1448×1086、RGBA、719734 bytes，SHA256 为 `799e52d96a3427ef11272974a1f2a1318fa1d32102dce445e079691fe36c12c4`。逐项来源、裁切框、处理范围与输出哈希见同目录 `PROVENANCE.json`。页面提供原 PNG、左右原照和炸弹参考切换，便于直接核对。

## 位置与附着

STAM 根据 `80_Days_China_2_1944.jpg` 的窗口下方位置登记；HUFF 根据右侧飞行照片的驾驶舱侧窗下方位置登记。炸弹与旗标行上移至名称上方。左右图案分别登记，文字、骰子没有通过镜像得到另一侧；嘴部与眼部依据两侧朝向设置图像横向读取方向，母体根尺度始终为正。

像素参考架原点在照片左上、u 向右、v 向下。局部机体架为 +X 左舷、+Y 向上、+Z 机头。图像区域与每项附着参数在 `runtime/80-days-r2.js`、`runtime/80-days-instance-r2.js` 中明确保存；图像坐标不是制造尺寸，未声称完成相机标定。

每块图案求交到指定同侧外表面，并转换到宿主部件局部坐标。临时显示面与 UV 在运行时生成，不存作对象几何事实。正面绘制阻止图案背面透过机首玻璃反向显现。0.002 工作米的表面偏移仅用于避免显示闪烁，不是实测油漆厚度。

## 默认状态与当前边界

螺旋桨默认显示，保留临时隐藏开关。展示默认正常透视，左右固定正交共用尺度、中心与上下基准；前后端、整机和移动窗口另作检查。原图预览与模型预览不宣称同尺度。

ROBBY 按用户指示暂缓。现有母体机首玻璃和窗口构型与照片仍有差异，图案在玻璃处留空；这会使机首局部嘴和眼部轮廓不完整，不能称为已经完成历史构型复原。没有通过改动原图字形、移动文字或覆盖玻璃来假装构型问题已解决。尾部沿用 R1，并保留未核实内容的候选开关。

本轮新版本使用固定提交公网 HTTPS 入口，实际浏览器完成资源和交互检查后交付；R1 与母体历史发布保持原样。本目录随附两份共同规则原件。
