# Weapons Mother：Aircraft 单线干净接续

用户最新决定：2026-09-05，否决并要求删除 S01，只沿 Aircraft 航空机枪版本接续。旧动画能提取的单独提取，不能复用的允许重新制作数字展示动画；弹壳、供弹部分和支撑以 Aircraft 版本及其适用资料为依据。先确定路线与正确原件，暂不制作新模型。

本入口覆盖此前学习记录和接续说明中关于保留 S01、恢复两套并行预览、必须等齐 R019 才能开工的旧计划。只影响 Weapons Mother，B24 整机、涂装及其他生产线保持原有规则。

## 已删除与禁止沿用

本次从 feature/b24-weapons-mother-v1 当前文件树删除 S01 的六个实验文件、两个专用构建 workflow 及一份交付说明，共九个文件。原页面、取件规则、相机朝向假设和显示修补链不得改名后再用。删除父提交为 4e42dc5e5f69ff72fb648fb93d3be88b7a420e94。

历史 Git 提交未强制改写；已经发出的聊天附件及旧 Actions 产物不属于此次文件树删除，它们全部退出接续与发布选择，不代表可接受版本。不得再将它们作为工作台入口或恢复候选推荐。旧工作台材料仅供按需核对、提取单个数字展示模块，禁止整套复制回新项目。

V016 的 distilled-reference.glb 属于旧的组合输出，不得拿它或 S01 中裁出的八个网格冒充干净的 Aircraft 原件。旧校准通过、字节保持、浏览器测试通过均不证明用户认可朝向。

## Aircraft 原件定位及状态

实际读到的历史来源清单：
- 仓库 haihao0307/AIRCRAFT。
- 固定读取提交 4e42dc5e5f69ff72fb648fb93d3be88b7a420e94。
- 文件 data/weapons-mother/b24-m2-aircraft-v016/distillation-manifest.json，role=anm2。
- 原件名称 anm2_browning_.50_cal_aircraft_machine_gun.glb。
- 清单记录大小 6,548,040 字节。
- 清单记录 SHA-256：2d6a1f323018523db42d1fe54dcf1a26661f139548134835779933d61ab68c8b。
- 清单标注来源为 Misja van Laatum 的航空 AN/M2 数字模型。作者与许可为历史记录，接入时保留并复核。

上述仅完成来源身份定位。本轮下载目录及已读 V016 归档未取得这份完整原件，不能声明原件已恢复、原姿态正确或生产基线已通过检查。文件库检索再次找到 R019 index.html 的引用记录，但没有据此恢复完整运行时及精确数据。

原始 GLB 可用作证据与无损提取输入；最终运行方式继续沿用已批准的精确数据蒸馏原则，不以基础体重画或改变文件名掩盖来源缺失。

## 下一次有限生产

先取得完整 Aircraft 原件并按来源记录检查，保留节点、UV、材料和组件清单。把前后、顶部、底部以及关键外观标记在同一坐标约定下核清；源空间到展示空间的转换只设一个明确位置，不继承旧页面的角度补丁。源文件的上轴文字声明仅作为待核线索，不能单独通过朝向验收。

首个展示对象为完整 Aircraft 航空机枪数字资产，不再安排旧版与 Aircraft 版的双项目按钮。先做正确朝向、组件完整、可环绕的静态三维版本，交用户确认；随后逐项加入供弹、弹壳和动画等可视化功能。

动画只提取可追溯且能适配新对象的独立展示逻辑。旧节点编号、补偿旋转、旧空间位置和不适用配件不自动带入。无法干净移植时，按用户授权重新制作数字展示动画，不为保留旧代码牺牲正确性。

弹壳、供弹与支撑先核对 Aircraft 参考中是否实际存在，以及来源的适用范围；存在的正确数据优先复用，缺失项明确记录。需要补件时只依据核实资料重做视觉资产，不补猜隐藏结构。具体飞机安装支撑要有对应飞机的证据，不能假定一套通用支架适合所有飞机。

材质学习记录中的通用方法可以重新实现，但不保留被否决的 S01 工作台、专用渲染实现或其姿态。此前对质感的肯定不等于接受 S01 的完整资产与结构。

## 本轮实际状态

仅执行 S01 活跃文件清理与接续决定更新；没有重建模型、改变原 UV、移植动画或发布新网页。新的完整 Aircraft 原件检查、朝向确认、浏览器验证、人工视觉接受均未执行。旧资产作为来源存档暂不批量删除，避免连带破坏用户允许提取的素材；新生产入口不依赖旧工作台。
