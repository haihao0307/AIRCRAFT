# Weapons Mother：Aircraft 单线规则生成接续

## 当前可查看版本：R01，2026-09-06

用户本轮要求实际可点击的 HTML 工作台。已新写 Aircraft 原生外观候选 R01，公开入口：

https://haihao0307.github.io/guilin-dem-pipeline/aircraft/weapons-native-r01/

本页源码位于 `native/weapons-visual/web/r01/`，实际构建并测试的提交为 `03087fefd77d87c70e2e16cf3b48ff99cd41f1ca`。发布在 guilin-dem-pipeline 的 gh-pages 独立目录 `aircraft/weapons-native-r01/`，未覆盖其他页面。当前 HTML 为 1,408,978 字节，SHA-256 `296853d3ad98c9d312fbeb45d6ddd1eed28e451e6e1b284812c6f2d0096f5960`。

R01 包含八个外观展示分组、独立支架候选、供弹外观、程序化 PBR 控制、选择/隔离/隐藏/展开复位、观察通道、同镜头 A/B、暂停/进度/重播，以及有限数量的弹壳、链节、闪光和曳光展示效果。统一采用 X 前方、Y 上方，原生根节点保持零旋转。部件可以在显示层展开；这不构成实物拆装说明。

形态来自新编写的紧凑形态规则，运行时生成临时几何。产品参考网格、源顶点/UV 数组、图片贴图都没有进入本页运行依赖或打包数据。通用 Three.js r170 与许可证内嵌在 HTML 中。

本页是第一版外观与交互候选，比例、瞄具、握持细节、供弹路径和支架适用性仍待进一步逐图核对及用户审阅。安装架参考博物馆 B-26G 可见外观，不能声明为 B24 指定配件。每五个事件一次曳光属于用户视觉预设，不是历史配弹结论。没有制作功能性内部机构、制造尺寸或真实武器性能模拟。

来源浏览器测试 run `34002025758` 已通过，1600×1000 与 390×844 两个 Chromium 视口各35项。Pages 部署 run `34002297787` 已成功。公开网址验证 run `34002166504` 的 HTML 字节核对和主交互测试已通过；独立展开无遮挡/离线检查的最终结果以该 run 的完整报告为准。最终交付回执位于 `reports/weapons-mother/native-r01-20260906/`。

## 继续有效的用户授权

2026-09-05，用户同意 Aircraft 航空机枪单线数字展示规划，进一步明确资料仅供参考，长期保存可读生成规则及自己的体系，不保留 mesh 和贴图。规则基础模块入口仍为 `native/weapons-visual/README.md`；其试片与先前检查记录属于基础阶段，不能再误认为当前只有试片，也不能将试片作为完整枪体交付。

S01 已删除且继续拒绝。禁止接回其场景、切件输出和九十度补丁链。禁止把旧顶点、索引、源 UV 表转成 JS/JSON/Base64 后称作规则蒸馏。旧无损访问器提取路线不作为本线后续生产方案。上述只影响 Weapons Mother，B24 整机、涂装及其他生产线保持各自原有规则。

## 持久数据边界

本线新目录保存语义部件、可解释参数、坐标约定、形态生成函数、部件关系、材质函数、展示状态、出处许可及检查结果。参考 mesh/贴图只可进入临时研究环境，不进入本线的新生产包或运行依赖，不通过压缩或改名隐藏。截图可以作为独立审阅证据，不作为产品贴图输入。

浏览器与 Blender 可在内存中生成临时网格、坐标、反射和阴影缓冲；这些不成为永久产品资产。未知结构不补猜为已核实事实。采用外部依据仍须保留来源与许可边界。

## 下一次继续工作

先打开并阅读 R01 公共入口、实际 HEAD、代码与发布回执，再按用户反馈进行有限修改。第一次视觉确认仍优先看方向、轮廓、组件完整、支架与瞄具关系。候选状态不自动提升为已接受，更不能把功能测试通过当作历史正确性。

保留当前可查看入口与回退点，新增修改使用明确版本。材质变化与几何变化分别记录。展开从原始位姿求值，不能从上一帧累加；展开时暂停演示；重新播放、切换检查模式及恢复参数时检查残留效果。原先两版并行和旧 R019 依赖不再是本线开工前置。

本轮发现并修复了粗糙度参数与 uniform 名称绑定错误，以及展开支架被展示台遮住的问题。展示台在展开时隐藏，产品坐标没有为掩盖遮挡而更改。先前失败测试不能当作最终测试；最终构建与发布字节需对应固定提交。

当前没有通宵自主开发代理或定时提醒。Actions 只执行实际提交的有限构建、测试与发布任务。不要承诺尚未发生的后台开发或自动完成。

## 历史来源线索

旧记录中的 Aircraft 原件名称为 `anm2_browning_.50_cal_aircraft_machine_gun.glb`，6,548,040 字节，SHA-256 `2d6a1f323018523db42d1fe54dcf1a26661f139548134835779933d61ab68c8b`。该记录只用于追溯；R01 没有读取或封装这份原件。不要据旧上轴声明、旧校准或旧网格保持检查认定新对象视觉已被接受。
