# Weapons Mother：Aircraft 单线规则生成接续

最新用户授权：2026-09-05。用户同意新的 Aircraft 航空机枪数字展示规划并要求开始执行，长期目标扩展为可复用机械数字资产体系。用户进一步明确：资料仅供参考和学习，最终不保留 mesh 或贴图，要把规律蒸馏为自己的体系。当前实施入口为 native/weapons-visual/README.md。

## 这条授权覆盖的旧计划

S01 已删除且继续拒绝。禁止接回其场景、切件输出和九十度补丁链。禁止把顶点、索引、源 UV 表转成 JS/JSON/Base64 后称作规则蒸馏。此前的完整原件保持、无损访问器提取等方案只可描述旧资料，不能继续成为本条新生产路线。只影响 Weapons Mother，B24 整机、涂装和其他生产线保持原有规则。

## 新体系保存什么

生产目录只保存可读生成规则、语义部件、可解释参数、坐标约定、部件关系、材质函数、展示状态、来源许可与检查结果。参考 mesh/贴图仅进入临时研究环境，不能进入新生产目录、运行依赖或交付包，也不能通过压缩或编码换名藏入。

浏览器和 Blender 可在内存里按规则生成临时网格、采样坐标和渲染缓冲，结束时释放；这些不成为永久资产。未取得外形或安装依据的部分继续标为未知，不用基础体拼凑冒称完成。

## 当前实际工作

已新写独立 native/weapons-visual 核心：参数化中性材质试片、绝对分件展示位姿、局部坐标粗糙度函数、包内资产检查和单元测试。试片只验证方法，不能作为新的枪体或工作台交付。Blender 接入脚本通过官方 bpy 进行有限实验，实际运行与否按 GitHub Actions 回执登记，不从脚本存在推定成功。

用户前轮确认的目标保留：完整 Aircraft 外观、独立有依据的机载安装架、可选择和展开复位的部件、PBR 材质、供弹/弹壳/链节等数字展示动画、每五个事件一次曳光的视觉预设。比例是用户的展示要求，不自动成为历史事实。分件属于非功能性观察，不提供实物制造、操作或功能性内部设计。

## 下一轮产物与验证

先从可靠外部参照建立顶部/底部/前后和部件语义，再编写自己的形态规则。统一坐标适配只有一处，独立零件不各自叠加纠正角。静态整体和安装架先核对，随后加入展开/复位、材质、完整展示状态。过去能复用的算法单独审查并重写为新接口，不能整包复活。

第一次检查应分别看完整性、正确朝向、材质与几何分离、反复展开复位、暂停/重播、源资料脱离后的重建及设备运行情况。模块测试通过不能代替完整工作台、真实浏览器和人工视觉验收。

明早九点是用户提出的检查时点，尚未登记固定时区或自动提醒。本会话没有通宵自主代理；Actions 只执行实际提交的有限构建/测试任务，结束后停止。不得据此承诺届时一定完成整套系统。

## 历史来源线索

旧来源记录给出 Aircraft 原件 anm2_browning_.50_cal_aircraft_machine_gun.glb，6,548,040 字节，SHA-256 2d6a1f323018523db42d1fe54dcf1a26661f139548134835779933d61ab68c8b。这里只保留追溯线索，未宣称原件恢复、默认上轴正确或用户批准。原件可以帮助研究，不能成为新系统的永久资产或唯一开工前置。
