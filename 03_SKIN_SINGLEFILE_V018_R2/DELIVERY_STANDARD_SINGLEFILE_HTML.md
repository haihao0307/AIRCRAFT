# 固定提交单文件 HTML 交付规范

用户于 2026-09-07 确认，今后每一版可视工作台按以下格式交付：

1. 每一版生成一个可独立运行的单文件 `.html`，运行所需的数据、样式和脚本全部内嵌。除非该版任务明确批准在线依赖，默认不得依赖分支可变地址、外部模型、贴图、CDN、兄弟目录或历史版本回退。
2. 文件必须先经过真实浏览器检查，再提交到 GitHub。交付链接必须使用完整的固定 commit SHA，禁止使用分支名、`main`、`latest` 或会继续变化的 Pages 路径冒充固定版本。
3. 聊天中提供 `raw.githack.com` 直开地址，格式为：

   `https://raw.githack.com/<owner>/<repo>/<full-commit-sha>/<path/to/file.html>`

4. 用户点击后可能先看到聊天客户端的“外部网站”确认页，再点击“打开链接”运行工作台。该确认页属于客户端安全提示，不能由工作台绕过。
5. 每版同时记录 commit SHA、HTML SHA-256、文件大小、验证结果、已知限制和人工接受状态。自动测试通过不能替代用户视觉确认。
6. 新版使用新路径或新固定提交。旧链接继续指向旧提交，不覆盖旧版，也不把旧版自动接入新版运行时。
7. 对单文件交付至少检查：桌面和 390×844 视口启动、无运行错误、无横向溢出、核心交互、数据身份、外部网络请求清单，以及源资产或几何保护边界。

本规范已应用于 `deliveries/b24-skin-static-v018-r2-singlefile/`。以后开工时先读取本文件，并以当前任务的实际边界补充专用检查。
