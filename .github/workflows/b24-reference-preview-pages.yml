name: Legacy B24 preview publisher disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "The Pages and cross-origin release-asset publisher is retired."
          echo "Use b24-reference-chunked-online.yml for the exact same-origin review page."
