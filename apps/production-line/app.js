const lineList = document.querySelector('#line-list');
function statusLabel(status){return {ready:'READY',pending:'PENDING',open:'OPEN',blocked:'BLOCKED'}[status]??status.toUpperCase()}
function renderLine(line){const article=document.createElement('article');article.className='line-card';article.dataset.status=line.status;const resources=(line.resources??[]).map(resource=>`<a class="line-resource" href="${resource.href}">${resource.label}</a>`).join('');article.innerHTML=`<div class="line-head"><span class="line-number">${line.number}</span><span class="line-state">${statusLabel(line.status)}</span></div><h3>${line.title}</h3><p>${line.summary}</p>${resources?`<div class="line-resources">${resources}</div>`:''}<div class="line-gate">GATE · ${line.gate}</div>`;return article}
async function start(){try{const [dashboardResponse,knowledgeResponse]=await Promise.all([fetch('./data/production-lines.json',{cache:'no-store'}),fetch('./data/knowledge-bases.json',{cache:'no-store'})]);if(!dashboardResponse.ok)throw new Error(`Dashboard registry request failed with ${dashboardResponse.status}`);if(!knowledgeResponse.ok)throw new Error(`Knowledge registry request failed with ${knowledgeResponse.status}`);const data=await dashboardResponse.json();const knowledge=await knowledgeResponse.json();document.querySelector('#updated-at').textContent=`UPDATED ${data.updatedAt}`;document.querySelector('#aircraft-label').textContent=data.aircraft.label;document.querySelector('#model-status').textContent=data.aircraft.modelStatus;document.querySelector('#runtime-status').textContent=data.aircraft.runtimeStatus;document.querySelector('#livery-status').textContent=data.aircraft.activeLivery??'NONE';document.querySelector('#knowledge-status').textContent=knowledge.knowledgeBases.length?`${knowledge.knowledgeBases.length} READY`:'NONE';const fragment=document.createDocumentFragment();for(const line of data.lines)fragment.append(renderLine(line));lineList.replaceChildren(fragment)}catch(error){const message=document.createElement('div');message.className='error-card';message.textContent=`Production registry unavailable: ${error.message}`;lineList.replaceChildren(message)}}
start();
