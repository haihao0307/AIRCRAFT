# Clean B24 livery workbench restart plan

Restart branch: `restart/b24-livery-workbench-clean-r1-20260907`.

The restart begins from the verified V018 R2 static skin single-file commit. It must remain a livery-only workbench. The compact animated aircraft and the accepted V017 baseline stay as read-only comparison and recovery sources.

## First production order

1. Restore the four propeller assemblies and their hubs as visible fit references. They remain outside the paint-target set until explicitly classified.
2. Keep both waist side doors closed by default and retain their exact open endpoints for later animation.
3. Add fixed port, starboard, nose, tail, top, bottom, and three-quarter cameras. Keep free orbit, pan, zoom, detail inspection, and stable part selection.
4. Build the livery system from explicit surface identity, side, local coordinates, and historical placement records. Do not restore the discarded source image/UV payload merely to paint the aircraft.
5. Treat 80 DAYS and UBANGI BAG III research snapshots as evidence libraries. Rejected or unfinished candidates do not become active by being present in the backup.
6. Keep the bomb-bay seam diagnosis visible as an open defect. The workbench may temporarily use the current source closed pose, but no patch strip or fake cover may be added. A later verified door-endpoint repair must be shared by animated and static lines.

## Delivery standard

Every review version is one self-contained HTML stored at a full immutable Git commit. Delivery in chat uses a raw.githack URL pinned to that complete commit. Desktop and 390 x 844 browser checks must run before the fixed link is issued.

`visualAcceptance` and `productionReady` remain false for every new candidate until explicitly changed by the user.
