These reference bytes are retained as read. wsd_binary_original.mjs is the original Xiaoma WSD0 container prototype. reviewed-codec-python.py is the exact earlier Python decoder hashed in Weather and Xiaoma receipts; the active codec additionally rejects duplicate required chunk names and unsupported required terms. The binary sample itself is unchanged. xiaoma-guidance.md is the theory guidance; its SHA is inside PROV.
