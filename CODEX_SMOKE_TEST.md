# Codex smoke test

Complete this small task to verify the rebuilt work environment.

1. Create `codex-smoke-test/result.json` with exactly these fields:
   - `status`: `"ok"`
   - `repository`: `"haihao0307/AIRCRAFT"`
   - `workflow`: `"ChatGPT-GitHub-Codex"`
   - `message`: `"Codex can read, edit, verify, and return changes through GitHub."`
2. Add a section named `## Codex connectivity` to `README.md` containing one sentence that says the smoke test passed.
3. Do not modify `AGENTS.md` or this task file.
4. Verify that `result.json` is valid JSON.
5. Summarize the changed files and verification result in your final response.
