# TASK 005 · 将炮塔系统接回权威 B-24 原模型

## 上游纠正

上一版 `b24-four-turret-v0.9.8.html` 使用程序化几何画了一架示意机。它只能说明控制概念，不能作为飞机生产线成果。该页面必须停用，不能继续作为默认入口、验收入口或历史飞机展示页。

本任务必须直接使用用户原先提供的权威 B-24 模型和既有飞机生产线，不得重新画飞机，不得用简化机身占位，不得用程序化飞机回退。

## 唯一允许的模型

- 文件名：`b-24_liberator.glb`
- 文件大小：`23085972` 字节
- SHA-256：`541c3dcfb98ab590cdb1bc90d6ddcdfe80bce2a4b937f3bccefab0c7efe8be0d`
- 场景：1
- 节点：1784
- 网格：348
- 三角面：325358
- 材质：30
- 内嵌图片：18
- 动画：1
- 动画通道：2518
- 来源模型 ID：`c64e58edfacb4c519b5602278a7e51aa`
- 作者：CreadorDeMu / Ricardo3D
- 许可：CC BY 4.0

模型边界：

```json
{
  "min": [-16.53828, -2.279375, -12.990289],
  "max": [16.538771, 2.724501, 7.86912],
  "extent": [33.077051, 5.003876, 20.859409]
}
```

任何字节数、哈希或清单不匹配的模型必须拒绝。禁止换用另一架 B-24。

## 原始生产线来源

优先源文件：

`B24_飞机生产线_v0.9.6_单一土质地面_隐藏大弧航线_投弹机枪版.html`

备用源文件：

`B24_飞机生产线_v0.9.5_真实模型内置版.html`

已有脚本 `scripts/extract-glb-from-html.mjs` 应负责从内嵌 HTML 恢复权威 GLB。若 Codex 环境中没有源文件，应依次尝试：

1. 仓库现有资产、历史分支、构建产物和 Release。
2. 官方 Sketchfab 模型下载接口或官方模型页面，仅接受下载后哈希完全一致的文件。
3. `source-input/` 资产桥。所有不依赖二进制的代码、测试、UI 和审计继续完成，但最终网页不得发布替代飞机。

最终成果必须把通过校验的 GLB 提交到：

`assets/model/b-24_liberator.glb`

该文件约 23 MB，低于 GitHub 单文件 100 MB 限制，可以直接进入仓库。提交后执行字节、SHA-256、GLB 结构和模型清单四重校验。

## 不可破坏的 v0.9.6 基线

必须保留：

- 一架权威 GLB
- 原节点层级
- 原材质与原贴图
- 原动画和 2518 条通道
- 四台螺旋桨的原始动画通道与既有时序
- 已校准的起落架收放端点、轮子和接地关系
- 单一黄色压实土质地面
- 隐藏的大弧形航线
- 机头跟随隐藏航线切线
- 起飞、爬升、巡航、投弹、进近、降落流程
- 四发尾烟逻辑
- 炸弹与机枪视觉效果位于 GLB 外部
- 涂装和炮塔修改全部可逆

禁止：

- 程序化飞机
- 简化飞机占位
- 复制一架新飞机覆盖原模型
- 手工猜测起落架轴
- 手工旋转螺旋桨替代原通道
- 更改原始模型比例
- 把炮塔动画写进原动画剪辑并覆盖原通道

## 炮塔工作范围

先对权威模型中已有的真实炮塔和机枪几何进行扫描，随后决定拆分方式。重点包括：

1. 机鼻炮塔或机鼻枪位
2. 背部上炮塔
3. 机腹球形炮塔
4. 尾炮塔
5. 左右腰部机枪位

每一处都要记录：

- node index 与名称
- mesh index
- 父链和子节点
- 材质
- 世界包围盒
- 几何连通岛
- 动画通道引用
- 是否可以独立隐藏
- 是否可以保持世界变换地重新挂接
- 是否与机身共享 primitive 或连续拓扑

拆分结论只能来自真实模型。按以下类型记录：

- A：独立节点或独立网格
- B：独立 primitive 或材质组
- C：同一网格中的独立连通岛
- D：与机身连续焊接，需要 Blender 精确切分

## 派生炮塔资产

原始 GLB 保持只读。若需要永久几何拆分，生成派生资产：

`assets/model/b-24_liberator_turret-rig-v1.glb`

该派生资产必须保留原始模型的视觉、材质、UV、法线、切线、动画层级和单位尺度，只重组已确认的炮塔组件。必须写出源模型哈希、派生模型哈希与变更清单。

目标层级示例：

```text
B24AuthoritativeRoot
  OriginalAircraftHierarchy
  TurretRuntime
    NoseTurretAssembly
      Yaw
        GunElevation
    DorsalTurretAssembly
      Yaw
        GunElevation
    VentralTurretAssembly
      Lift
        Yaw
          BallShell
          GunElevation
            GunLeft
            GunRight
    TailTurretAssembly
      Yaw
        GunElevation
    WaistGunPort
    WaistGunStarboard
```

所有枢轴必须从真实几何计算并通过网页近景检查，不能沿用程序化示意机的猜测坐标。

## 动画和交互

把已完成的炮塔控制合同接到真实几何：

- 炮塔选择
- 水平旋转
- 双枪同步俯仰
- 机腹球形炮塔收放，仅在模型结构和史实支持时启用
- 自动扫描
- 虚拟目标跟踪
- 枪口节点跟随
- 后坐预览
- 临时脱离
- 重新挂接
- 完整复位
- 二十秒演示

运动范围、旋转轴和遮挡限制需要按每个真实炮位分别定义。不得让枪管穿过机身、尾翼、驾驶舱或炮塔壳体。

## 网页成果

最终网页：

`b24-authoritative-turrets-v0.9.9.html`

它必须从同一仓库加载：

`assets/model/b-24_liberator.glb`

网页要求：

- 打开即显示用户原来的真实 B-24
- 默认不显示任何程序化飞机
- 权威模型校验失败时显示错误报告并停止渲染
- 可切换整机、机鼻、背部、机腹、尾部、左右腰部近景
- 可查看节点与网格绑定
- 可操作真实炮塔和真实机枪几何
- 可播放原始飞机动画
- 可播放炮塔动画
- 可执行完整飞行流程
- 可一键恢复模型初始状态
- 桌面端和移动端可用
- 控制台零错误

仓库根入口 `index.html` 最终指向该页面。`b24-four-turret-v0.9.8.html` 改成停用说明或重定向，不再绘制程序化示意机。

## 自动验证

新增并通过：

1. 权威模型字节与哈希测试。
2. GLB 清单测试。
3. 禁止程序化飞机测试。正式页面与生产入口中不得出现用 Box、Cylinder、Sphere 等拼装完整飞机的代码。
4. 炮塔映射完整性测试。
5. 原动画通道保持测试。
6. 螺旋桨、起落架、轮子、烟雾、投弹和飞行阶段回归测试。
7. 炮塔枢轴、范围、穿插和复位测试。
8. 静态构建与网页部署测试。

## 完成定义

只有同时满足以下条件才算完成：

- 精确 23,085,972 字节 GLB 已提交到仓库
- SHA-256 完全匹配
- 网页显示的确实是该 GLB
- 炮塔与机枪来自该 GLB 或其可追溯派生资产
- 程序化示意机已经从正式入口撤下
- 测试与构建通过
- 修改已推送到此分支
- PR 中有完整 handoff、截图和测试结果
- 合并后在线网址可直接打开

如果权威二进制仍未获得，任务状态必须写为 `blocked-on-authoritative-binary`，不得宣称网页完成，不得发布替代飞机。
