# TASK 010 · B24 模块化整机装配与垂直尾翼试验

## 任务目标

在现有 B24 工程图与 CAD 知识库之上，建立一套“逐部件生产，最终可靠装成整机”的固定方法，并用垂直尾翼与方向舵完成第一轮端到端试验。

本任务同时验证四件事：

1. 整架飞机拥有统一坐标、基准、站位和装配树。
2. 每个部件可以独立研究、建模、审核、替换和版本化。
3. 程序化几何、部件层级和网页视觉校验全部由仓库原生工具实现。
4. 工程 CAD、来源证据和最终运行模型之间保持可追溯映射。

## 当前分支与边界

继续在 `research/b24-engineering-drawings-cad-v1` 工作，并继续使用 Draft PR #14。

本轮不替换权威 `b-24_liberator.glb`，不改动生产页面，不宣告任何新几何达到制造、适航或结构安全用途。所有自动重建结果保留为派生候选，直到工程来源、尺寸和变型审核通过。

## 整机总概念

整机根对象为 `B24J_CO_ENGINEERING_MASTER`。任何部件生产开始前，先服从以下共同规则：

- 全机只有一套工程坐标系、运行时坐标映射和单位。
- 全机只有一棵规范装配树。
- 每个部件拥有稳定 `component_id`。
- 每个部件声明父装配、装配接口、局部原点、方向、适用变型和来源。
- 左右件分别登记；只有来源明确证明对称时才允许共享几何定义。
- 活动件必须声明轴线、枢轴、运动范围来源和静止基准。
- 外表面必须保留蒙皮、面板、铆钉、开口和涂装锚点映射。
- CAD、运行时网格、GLB 和纹理只能通过受控导出互相映射，不能互相静默覆盖。

## 工程与运行时坐标

工程坐标暂定采用航空器机体坐标语义：

- `X`：纵向，向机头为正。
- `Y`：横向，向右舷为正。
- `Z`：竖向，向上为正。

正式原点、机身站位、基准水线和纵剖线必须等待原厂总体图、站位图或坐标表确认。当前所有原点值标记为 `provisional`。

Three.js 映射采用显式矩阵，禁止在各部件代码中自行交换轴：

- `three.x = engineering.y`
- `three.y = engineering.z`
- `three.z = engineering.x`

## 首个试验部件

选择 `vertical-tail-assembly`，包含：

- 左垂直安定面
- 左方向舵
- 右垂直安定面
- 右方向舵
- 铰链轴与枢轴
- 与水平尾翼和后机身的安装接口
- 外蒙皮和尾号、三角队徽等涂装锚点

选择它作为首件的原因：

- 外轮廓在侧视、正视和俯视资料中容易辨认。
- 固定面与活动面的关系可以测试部件拆分和铰链轴。
- 左右双垂尾可以测试对称复用规则和独立适用性。
- 尾部标记可以测试工程曲面到历史涂装坐标的映射。
- 装配接口可以验证局部部件最终回到整机坐标的能力。

## 原生重建与验证职责

1. 从已标定的三视图中提取可追溯轮廓与控制尺寸。
2. 将固定尾翼、方向舵、铰链和安装接口写入稳定组件树。
3. 由仓库原生生成器输出节点、插槽、枢轴和运行时网格。
4. 使用固定视角、固定时刻和可重复几何门槛检查轮廓、穿插、手性和装配偏差。
5. 任何无法从证据确认的尺寸、材料、内部结构和变型适用性继续保持 `pending_source`。

## 试验流程

### 阶段 A · 来源准备

1. 入库 B24J 三视图、Model 32 总体布置图、尾翼相关结构修理页和零件目录页。
2. 记录文件名、字节数、SHA256、页码、图号、修订和适用变型。
3. 提取垂尾与方向舵控制尺寸、剖面、铰链和安装关系。
4. 将现有权威 GLB 作为 E 级视觉比较对象，禁止从其网格反推工程尺寸后直接批准。

### 阶段 B · 整机装配骨架

1. 建立全机装配根和尾部父装配。
2. 建立 `vertical-tail-assembly` 左右接口。
3. 固定局部坐标、轴向和命名。
4. 在没有完整几何时先用基准点、轴线和包围盒占位，避免各部件采用不同坐标。

### 阶段 C · 原生轮廓与组件验证

1. 从标定图纸生成独立正投影轮廓掩膜。
2. 固定面和活动面分别建立组件。
3. 记录所有不可见区域和自动推断区域。
4. 逐阶段完成 blockout、structure、form、material、interaction 和 optimization 检查。
5. 生成前、后、左、右、上、下和四个斜视角的网页证据。

### 阶段 D · 工程 CAD

1. 用经过审核的尺寸和剖面建立参数化曲线。
2. 建立安定面和方向舵的外形理论面与实体候选。
3. 建立铰链轴、缝隙和安装基准。
4. 输出参数文件、STEP 和投影检查图。
5. 将每条关键曲线和尺寸关联到 `source_id`、页码或图框区域。

### 阶段 E · 交叉校验

1. 对比工程 CAD 投影、原厂图纸和权威 GLB。
2. 任何差异分别归类为来源冲突、扫描畸变、变型差异、原生重建误差或现有模型误差。
3. 工程 CAD 通过后，从 CAD 重新生成运行时网格。
4. 将固定尾翼和方向舵装回整机装配树。
5. 验证方向舵运动时不穿插、轴线不漂移、左右关系正确。

### 阶段 F · 涂装与零件生产接口

1. 为尾号、队徽、板缝、铆钉和检修盖板建立表面锚点。
2. 每个锚点同时保存 CAD 曲面参数、工程坐标和运行时表面映射。
3. 固定面与方向舵拥有独立涂装区域，禁止跨铰链连续烘焙造成拉伸。
4. 零件导出保留装配基准和来源链。

## 验收门槛

1. `assembly-master.json` 可解析，并含完整坐标和装配树。
2. `vertical-tail.json` 可解析，并含左右件、固定面、活动面、铰链和安装接口。
3. 所有当前未知尺寸明确标记 `pending_source`，禁止填入猜测值。
4. 原生构建输出保留仓库版本、输入哈希、参数和未解析区域声明。
5. 三视图轮廓代理在每个已提供视图中分别通过，不允许只用一个平均分掩盖某个方向的错误。
6. 左右垂尾分别检查，镜像复用必须有明确批准记录。
7. 方向舵枢轴来自组件接口，禁止以视觉中心临时旋转。
8. 工程 CAD 的所有控制尺寸均可回溯到 A 级或 B 级来源。
9. 生成的 STEP、GLB、TypeScript 和纹理映射均带版本、父来源和哈希。
10. 当前 `engineeringMasterApproved` 和 `verticalTailApproved` 均保持 `false`，直到真实图纸入库和浏览器验收完成。
