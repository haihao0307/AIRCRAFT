# Aircraft knowledge system

`knowledge/` contains bilingual, source-traceable research packages shared by aircraft identity, crew history and historical livery production.

Each package keeps a stable entrypoint, structured knowledge JSON, a source registry, local project originals where permitted, and evidence-grade boundaries. The production dashboard discovers packages through `data/knowledge-bases.json`.
