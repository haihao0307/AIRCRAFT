# 80 DAYS R6 · SELF AUDIT

Status: `self-rejected-registration`

R6 is retained as evidence that the data architecture improved while the registration hypothesis remained wrong.

What is worth keeping:

- Mother 01 remains frozen and unmodified.
- Port-side reconstructed elements share one E04 `2000 x 1243` photo-pixel coordinate sheet instead of being individually hand-positioned.
- R3/E04 mouth perimeter, recovered tooth contours, photographed dice faces/pip contours, ROBBY and HUFF remain evidence-bound.
- World-space skin shader projection is performant and avoids a heavy raycast decal mesh.
- Desktop and `390 x 844` mobile browser QA passed.

Why R6 is rejected visually:

- The `component 69` single-window identity is not proven.
- In fixed screenshots the entire E04 sheet becomes too small/low: the mouth is a narrow low wedge, the dice sit too far aft relative to the historical composition, and ROBBY/HUFF become implausibly tiny/displaced.
- Therefore no element should be individually nudged to compensate. The failure is at the photo-to-aircraft registration level.

Do not reuse as truth:

- Do not lock `component 69` as the photographed E04 window.
- Do not call the 1.73 m mouth width historical truth.
- Do not alter the recovered mouth/dice/name paths merely to make this transform look better.

Next gate:

1. compare multiple Mother-01 glass components against the same E04 coordinate sheet;
2. identify at least two independent structural correspondences, or record a nose-configuration mismatch;
3. only then lock a projective/curvilinear registration and restore title/eye/mission state.

`visualAcceptance=false`
`productionReady=false`
